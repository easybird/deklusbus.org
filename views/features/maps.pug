#maps
    .col-md-9
        #map-container.z-depth-1.wow.fadeInUp(style="height: 500px")

    .col-md-3
        ul.text-xs-center
            li.wow.fadeInUp(data-wow-delay='0.2s')
                i.fa.fa-map-marker
                p Tielt, 8700
            li.wow.fadeInUp(data-wow-delay='0.3s')
                i.fa.fa-phone
                p + 32 491 73 75 22
            li.wow.fadeInUp(data-wow-delay='0.4s')
                i.fa.fa-envelope
                br
                a(href="mailto:gijs@deklusbus.org") gijs@deklusbus.org

    script(src="http://maps.google.com/maps/api/js")
    script(src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAoHQqXvXtnKmX4LAp_SMNDI0ugdMsXZF0&callback=initMap" async defer)
    script.
        function init_map() {
            var var_location = new google.maps.LatLng(51.001000, 3.328280);

            var var_mapoptions = {
                center: var_location,
                zoom: 10
            };

            var var_marker = new google.maps.Marker({
                position: var_location,
                map: var_map,
                title: "Tielt"
            });

            var var_map = new google.maps.Map(document.getElementById("map-container"),
                    var_mapoptions);

            var_marker.setMap(var_map);

        }

        google.maps.event.addDomListener(window, 'load', init_map);
